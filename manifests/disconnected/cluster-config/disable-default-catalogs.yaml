# Disable default OperatorHub catalog sources.
# They point at registry.redhat.io and will never resolve in a disconnected cluster.
# Apply AFTER pushing to the mirror registry and BEFORE installing operators.
apiVersion: config.openshift.io/v1
kind: OperatorHub
metadata:
  name: cluster
spec:
  disableAllDefaultSources: true

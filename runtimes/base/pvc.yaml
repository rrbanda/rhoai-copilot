apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rhoai-copilot-data
  labels:
    app.kubernetes.io/part-of: rhoai-copilot
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi

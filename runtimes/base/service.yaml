apiVersion: v1
kind: Service
metadata:
  name: rhoai-copilot
  labels:
    app: rhoai-copilot
    app.kubernetes.io/part-of: rhoai-copilot
spec:
  selector:
    app: rhoai-copilot
  ports:
    - port: 18789
      targetPort: 18789
      protocol: TCP
      name: dashboard
  type: ClusterIP
